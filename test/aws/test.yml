version: 2.1

jobs:
  build:
    executor: terraform/default
    steps:
      - checkout
      - run:
          name: Create Sample Terraform
          command: |
            mv test/aws .
      - terraform/init
      - terraform/plan
      - terraform/apply:
          use-plan: false
